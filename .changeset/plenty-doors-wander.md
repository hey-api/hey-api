---
"@hey-api/openapi-python": patch
---

**plugin(@hey-api/python-sdk)**: convert a parameter's name from the spec into a valid Python identifier, keeping the name from the spec in the field's `map`
