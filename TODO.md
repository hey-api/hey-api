- [ ] Implement enum generation fix for #4266: suppress per-member JSDoc/comments caused by `x-enum-varnames` while preserving enum `schema.description`.
- [ ] Ensure enum member identifiers still use `x-enum-varnames`.
- [ ] Run targeted vitest for `enum-varnames-comments.test.ts`.
- [ ] If formatting/lint issues appear, apply minimal fixes.
- [ ] Prepare branch + PR creation (blackboxai/ prefix).

