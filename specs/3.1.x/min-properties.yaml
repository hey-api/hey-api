openapi: 3.1.0
info:
  title: minProperties reproduction
  version: 1.0.0
paths: {}
components:
  schemas:
    ExampleRequest:
      type: object
      required:
        - options
      properties:
        options:
          type: object
          minProperties: 1
          maxProperties: 1
          properties:
            optionA:
              type: boolean
            optionB:
              type: boolean
